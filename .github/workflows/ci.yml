name: CI

on:
  push:
    branches: [main]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v7

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Restore
        run: dotnet restore Crestful.slnx

      - name: Build
        run: dotnet build Crestful.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test Crestful.slnx --configuration Release --no-build
